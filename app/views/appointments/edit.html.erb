<h1>Editar <%= @cocktail.name %><h1>
<div class="container">
 <%= simple_form_for([user]) do |f| %> 
    <%= f.input :appointment_date, wrapper_html: { class: 'flex-select' }, as: :date, html5: true %>   
    <!-- deplegable de horios-->
    <%= u.label :start_hour, "Appointment Begin:"  %>
    <%= u.text_field :appointment_date,class:"form-control" %> 
    <%= u.submit class:"btn btn-dark" %>
  <% end %>
</div>
<%= link_to "volver", appointments_path %>